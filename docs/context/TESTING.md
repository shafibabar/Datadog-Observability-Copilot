# TESTING.md — TDD discipline & metrics log

## Protocol (binding)
- **Regression invariant:** once a test is green it stays green. Every step runs the full suite (`pytest`); previously-passing tests must never break.
- **Test-first:** for each new capability, write the failing spec tests first (red), then implement to green. (The Iteration 0 *foundation* tests were retrofitted — noted honestly below — everything from the reasoning engine onward is genuinely test-first.)
- **Honest metric:** each step reports `passing / total (%)`. If the pass % drops *because new red spec tests were added for a new requirement*, that is stated explicitly — it is expected TDD behavior, not regression.
- **Pending tests** (spec written ahead of implementation) are marked `@pytest.mark.pending` so red-by-design specs are mechanically distinct from real breakage.

## How to run
```
source .venv/bin/activate
pip install -r requirements-dev.txt
pytest                 # full suite
pytest -q              # quiet
pytest tests/test_config.py::test_defaults_when_unset   # a single test
pytest --cov=app       # with coverage (historically ~99% on the product app)
```

**Test layers:** unit (models, guard, scope, adapters, store, personas, artifacts — Datadog HTTP mocked, LLM faked) · functional (FastAPI endpoints via `TestClient` with a fake-LLM `Copilot` over replay data) · declarative **UI-contract** (`tests/test_web_ui.py`, asserts served markup/JS). Optional real-browser smoke: `tests/test_smoke_playwright.py` (SKIPPED unless `playwright` + a browser are installed).

## Metrics log (newest first)
| Date | Step | Passing | Total | Pass % | Notes |
|---|---|---|---|---|---|
| 2026-08-05 | Live-Datadog defects: `DATADOG_ENV_TAG`, registry-driven resolver, bounded timeline | 377 | 378 (1 skip) | 100% | Test-first, +17. Third probe run returned data and exposed four defects invisible to the offline suite. **(a) `DATADOG_ENV_TAG` (new config, 6 specs):** the org carries **no `env` tag** (`by {env}` → the placeholder series `env:N/A`); environment is `kube_namespace`, tenant is `tenant`. `"env"` was hardcoded in 4 adapter sites, so every environment filter would have matched nothing *silently* (a wrong tag returns HTTP 200 with zero series). Specs cover scope filter, tag discovery, env-narrowed tenant discovery, event tags, the `env` default, and `N/A` being stripped from discovered values. **(b) Resolver keyed off the live registry (5 specs):** `select_metrics` opened with `if not index.metric_queries: return []` — with `MONITORS_REPO_PATH` unset the Terraform index is empty while discovery yields 420 metrics, so it selected **nothing** and the engine fell through to `metrics=None` = query-all-420. `available` is now the authority; `index.aliases_from_metric_names` supplies vocabulary without Terraform; engine bounds when the registry exceeds `DEFAULT_TOP_K` **or** a Terraform index exists (2 specs). **(c) `build_timeline` bounded (2 specs)** to `MAX_TIMELINE_EVENTS=60` — the events API returns ~1000/hour, so the Workspace timeline shipped 1000 rows per reply; critical/warning kept ahead of routine noise, result still chronological. **(d) 2 config specs** for both tag keys + `/api/status`. Coverage 98%. **Verified live end to end** (not just green): 420-metric registry, 8 metric series with data per question, a real LLM investigation citing 45 evidence entries. Zero regressions. |
| 2026-08-05 | Registry quality from probe data: sub-metric filter + confirmed-reporting rule | 360 | 361 (1 skip) | 100% | Test-first, +15. First live probe **confirmed** `GET /api/v1/metrics` (16,117 names, 1,297 `ec.*`) and exposed two registry-quality problems. **Generated sub-metrics excluded** (`namespaces.is_stat_submetric`, 6 specs): `.count/.sum/.min/.max/.avg/.median/.NNpercentile` are Datadog artifacts of one distribution — 877 of the 1,297 names, leaving 420 distinct signals; matched as dotted suffixes so `…counter`/`…max_depth` are safe; explicitly-configured names bypass. **Extracted metrics must be confirmed reporting** (5 specs): the probe's Terraform sample metric returned 0 series and 0 tags (in the `.tf` files, not emitting — 137 of 320 are dead), so `merged_metric_queries` now intersects extracted with discovered whenever discovery returned anything; discovery returning nothing keeps the full extracted set (graceful degradation). 3 fixtures corrected — they had used `ec.*.count`-style names that the new rule (rightly) treats as generated. Probe restructured to discover **first** and target a confirmed-live metric; its old ordering had made 4 of 7 steps silently inconclusive. Coverage 98%. Zero regressions. |
| 2026-08-05 | Metric scope: `DATADOG_METRIC_NAMESPACES` replaces `DATADOG_DISCOVERY_METRIC` | 345 | 346 (1 skip) | 100% | Test-first, +46 (new `tests/test_namespaces.py`, 17). New pure `app/telemetry/namespaces.py` (stdlib `fnmatch`, **no new dependency**): `parse_patterns` (`"ec.*, ea.*"`; bare `ec`/`ec.` → `ec.*`), `matches` (case-insensitive; **empty patterns match everything**, so unset config is byte-for-byte today's behavior), `prefixes`, `filter_queries(keep=…)`. Applied as an allowlist to all three metric sources: new best-effort `discover_metric_names` (9 specs incl. HTTP-error/unexpected-shape → `[]`, no-namespaces → no call), Terraform extraction now prefix-parameterized (4 specs), and the merge. `DATADOG_DISCOVERY_METRIC` **removed** — tag enumeration groups an in-scope registry metric instead (3 specs, incl. empty registry → no HTTP call). Guard vocabulary derives from the registry via `index.service_vocabulary` (3 specs, incl. dropping <4-char segments that would fast-allow every message — a defect found in my own new code). UI: `@` menu hides a dimension with no options (1 spec). **Hermeticity fix:** `_clear()` in `test_config.py`/`test_copilot.py` now unsets `DATADOG_*`/`MONITORS_REPO_PATH` — a populated `.env` genuinely broke `test_config.py`. Coverage 98%. Zero regressions. |
| — | *(gap: the 279 → 299 correlation-layer/merge session was never logged here by that session; see STATE.md and DECISIONS.md 2026-07-13 for it)* | 299 | 300 (1 skip) | 100% | Recorded retrospectively as a known hole in this log, not as a verified per-step entry. |
| 2026-07-27 | Platform-narrowed scope, Phase 2 (`@` menu + contenteditable composer) | 279 | 280 (1 skip) | 100% | Removed `#scope-trigger`/`#scope-panel`; composer (`#input`) rebuilt as `contenteditable`. Typing `@` opens a caret-anchored `#at-menu` (Environment/Tenant/Duration, reusing the existing presets + custom-range modal); a selection inserts a locked, non-editable chip (`contenteditable="false"`, click-to-remove). `composerText()`/`scopePayload()` read the message and the scope independently from the composer's DOM — never one parsed from the other; an empty selection reuses the conversation's existing/default scope rather than resetting it. Persona now a plain visible `<select>`. `tests/test_web_ui.py` rewritten for the new contract; `tests/test_smoke_playwright.py` rewritten to exercise typing `@`, drilling into Duration, and chip insertion. **Real-browser run attempted (installed Playwright + Chromium) but blocked by missing system shared libraries requiring root `apt install` — not verified interactively.** Coverage 98%. Zero regressions in the offline suite. |
| 2026-07-27 | Platform-narrowed scope, Phase 1 (backend) | 274 | 275 (1 skip) | 100% | `COPILOT_PLATFORM_*` config (environments/tenants/metrics/log sources/trace services/default window). `Scope.validation_error()` relaxed: no forced env/tenant selection, no forced duration (only an inconsistent partial duration or over-7-day span is rejected). `Copilot._with_defaults()` backfills missing scope fields from platform config (or an unfiltered 2-day window) and persists the result. `guard.evaluate()` gained `extra_vocabulary` sourced from the platform config, recognized alongside the generic on-topic word list. `list_scopes()`/`/api/scopes` serve the static platform config directly, bypassing live discovery, once configured. `scripts/check_env.py` reports the resolved platform config. Coverage 98%. Zero regressions. |
| 2026-07-08 | Scope menu (Claude-style drill-down) + submenu-close fix + Playwright smoke | 243 | 244 (1 skip) | 100% | Replaced the four scope pills with one `#scope-trigger` opening a drill-down popover (Environment/Tenant/Duration/Explain-as → option lists; multi-select checkmarks + filter for env/tenant; personas with descriptions). Hidden `<select>`s remain state holders. **Bug fixed:** drilling in rebuilt the panel, detaching the clicked row so the bubbled click hit the outside-close handler and shut the menu (empty submenus) — fixed via `stopPropagation` on `#scope-panel`. Added opt-in `tests/test_smoke_playwright.py` (real browser; skips without playwright+browser). UI-contract tests updated. |
| 2026-07-08 | Scope/UX UI + .env diagnostics + asset cache-busting | 242 | 242 | 100% | Control row below the composer (env/tenant/duration/persona), conversation rename/delete, resizable+collapsible panels, per-reply copy; **pure-CSS flex layout** (renders with no JS) after a JS-grid layout broke twice. `main.py` serves index with mtime-versioned CSS/JS + `no-store` so a browser can't reuse stale assets. `scripts/check_env.py` + `dotenv_path`/`dotenv_loaded` in `/api/status` (diagnosed the ".env not loading" report as a config-contents issue, not a loader bug). |
| 2026-07-08 | Scoped investigations backend (Scope model + discovery) | 231 | 231 | 100% | Test-first, +35 (incl. new `tests/test_scope.py`). `Scope{env,tenant,window}` persisted per conversation (`scope_json` + migration), overridable per turn, threaded engine→evidence→source. Datadog builds `{(env..) AND tenant:..}` filters + `list_scopes` discovery (env→tenant narrowing) + configurable `DATADOG_TENANT_TAG`; Replay ignores scope. Store delete_workspace; Copilot summary-subjects + rename/delete; API scope-validation (`400`), `/api/scopes`, `PATCH`/`DELETE`. |
| 2026-07-08 | Relevance & abuse guard (pre-reasoning gate) | 196 | 196 | 100% | Finished a red-ahead spec left in the tree. `app/guard.py` `evaluate()`→`GuardVerdict`: deterministic Stage 1 (block empty/over-long/injection; fast-allow on-topic + short in-context follow-ups), hybrid Stage 2 fails closed. Wired into `Copilot.ask` before persist/reason; `_SYSTEM` hardened to treat inputs as untrusted; `COPILOT_GUARD_*` settings. |
| 2026-07-03 | Fix: tabs didn't switch → pure-CSS radio tabs | 171 | 171 | 100% | **Escaped defect + test-gap fix.** The Timeline/Overview switch was JS-driven and only the HTML *presence* of tabs was tested, not the switching behavior — so a broken switch shipped (JS logic was actually correct; the live browser ran stale cached JS). Reworked switching to a **pure-CSS radio hack** (`:checked ~ #tab-view`) so it works with no JS at all; JS now only re-renders the visible tab's canvases (graceful degradation). +3 tests that assert the switch declaratively and would have caught this: two radios with exactly one default-checked; radios-before-views sibling order (required by `~`); and the `.tabview{display:none}` + `#r-*:checked ~ #tab-*` reveal rules present in the CSS. Verified via HTTP (rules served) + a Node DOM harness (visible-tab routing). Zero regressions. |
| 2026-07-03 | Collector records current test total | 168 | 168 | 100% | Test-first, +4 specs. Root cause: the collector never populated `tests_run/passing/failing`, so every live impl record wrote `0` (the `113` peak was purely from reconstructed history). Fix: `count_tests(repo)` — a stdlib-only static count of defined `test_…` functions under `tests/` (missing dir → 0) — wired into `git_turn_stats` as `tests_run == tests_passing`, `tests_failing = 0`. Chosen over running pytest in the Stop hook (needs venv, adds latency, could record red mid-edit); green invariant makes defined == passing. Verified on the real repo (168) and that `build_record` surfaces the values. Zero regressions. |
| 2026-07-03 | Metrics dashboard: Timeline (by-day) tab | 164 | 164 | 100% | Test-first, +10 specs (analytics/API). Rows now carry `date`+`prompt_ts` (missing ts → `"unknown"`). `by_day`: chronological grouping (`"unknown"` last), per-day metric sums, per-day peak + cumulative-peak tests (carry-forward), monotonic cumulative cost (== summary total), and `timeline_summary` (first/last/active_days/busiest_day). Robustness: missing-timestamp records bucket to `"unknown"` without crashing; empty → empty timeline. FastAPI: `/api/metrics` includes `by_day`+`timeline_summary`; `/` HTML has both tabs. Frontend (tabs, `renderTimeline`, per-prompt timestamp table) boot/HTTP-smoke-tested per convention, not unit-tested; `charts.js` untouched so Tab 1 is byte-for-byte unchanged. Zero regressions. |
| 2026-07-03 | Keyless Claude CLI backend + Datadog PAT | 154 | 154 | 100% | Test-first, +11 specs. `ClaudeCliClient` (5): invokes CLI + selects deep/fast model + passes system/user + trims stdout, surfaces runner failure, real-process runner success + non-zero→RuntimeError, `cli_available()` never raises. Config (2): `llm_backend` default `auto`+lowercased, `has_datadog` true with access token alone. Datadog (1): access token → `Authorization: Bearer`, legacy key headers absent. Copilot (3): keyless+no-CLI→None, keyless+CLI→CLI-backed Copilot, `sdk`-backend needs key; `_build_source` selects Datadog via token. App (2 rewritten): degradation path forced via monkeypatched `build_copilot` (no longer env-dependent now keyless can succeed). Subprocess + CLI detection injected — no real CLI spawn, no network. Coverage **99%** (llm/config 100%). Zero regressions. |
| 2026-06-27 | Metrics subsystem (collector + dashboard) | 143 | 143 | 100% | Test-first, +30 specs. Collector (16): transcript→cycles, intent, git-output parsing, per-turn delta arithmetic incl. untracked, dedupe, record shape, real temp-git integration. Dashboard data layer (14): tolerant loader (skips malformed/blank), normalize defaults + schema-evolution tolerance, aggregations, cost estimate, FastAPI `/api/metrics` (survives malformed file / missing file). Stdlib-only collector; FastAPI dashboard reuses existing stack. Zero regressions. |
| 2026-06-26 | Iteration 1: conversations + memory + UI | 113 | 113 | 100% | Test-first: +15 net specs. Engine conversational memory (history fed to prompt, optional + bounded); store messages/conversations (roundtrip, isolation, title, recency ordering, activity bump); section serializer (typed + JSON-safe); conversation-aware Copilot (isolation, memory, titling, persist turns, rerender-no-LLM, artifact, JSON-safe); conversation API (create/list/get/chat/artifact, 404/400/503). `test_copilot.py` and `test_app.py` rewritten for the new API. Frontend boot + response-shape smoke-tested (not unit-tested). Coverage **99%**. Zero regressions. |
| 2026-06-26 | Refactor + hardening pass | 98 | 98 | 100% | Refactor (behavior-preserving, existing tests guarded it): `rank` ordering moved onto `Confidence`/`Severity` enums (removed dup rank-maps in artifacts); shared `outstanding_questions()` helper (de-duped between sections + artifacts); type-safe `EventSource.SUPPORT` compare. +16 specs closing real gaps: enum `rank` ordering, `workspace_db` config, `reasoning_objects(snapshot_seq=)` filter, confidence-history isolates distinct hypotheses, **security guard (no secret bytes in workspace DB)**, artifact empty-investigation degradation, `build_default_session`/`_build_source` production factory (replay/datadog/fallback/keyless), `extract_json` malformed branch, engine rejects non-object JSON, `/api/artifact` 503-without-session, lazy session build+cache. **Coverage 99%** (main/copilot/engine 100%). Zero regressions. |
| 2026-06-26 | Incident Summary artifact | 82 | 82 | 100% | Test-first: +11 specs (registry contains incident_summary, unknown key raises KeyError, audience + core sections present, picks highest-confidence hypothesis (not the LOW one), severity derived from timeline, grounded transform (summary + recommendation verbatim), to_markdown title/headings; CopilotSession.artifact serializes from latest snapshot with NO llm call + investigates-if-empty; `/api/artifact` generates summary + rejects unknown key with 400). LLM faked. Zero regressions. |
| 2026-06-26 | Chat endpoint wired | 71 | 71 | 100% | Test-first: +14 specs (persona registry has the 5 charter personas, persona=config-lens, get_persona default-to-sre, render grounded in investigation, render differs by persona on same facts, leadership more concise than sre, pm surfaces customer-impact+recommendation; CopilotSession creates workspace + records snapshot, second ask appends history, reply ships evidence, persona-switch re-renders with NO llm call, rerender-without-prior runs one; app `/api/chat` investigates when session wired + empty-message re-render). LLM faked, ReplayAdapter, in-memory store — no key/network. Zero regressions. |
| 2026-06-26 | Investigation Workspace | 57 | 57 | 100% | Test-first: +14 specs (create/get workspace, append-only snapshots w/ incrementing seq, history preserved, latest, full Investigation roundtrip, persistence across store instances, queryable reasoning atoms, confidence-over-time per hypothesis, hypothesis-key normalisation, registry covers 11 charter sections, ordered+titled render, section population, affected-services derivation). SQLite; no LLM/network/keys. Zero regressions. |
| 2026-06-26 | Reasoning engine | 43 | 43 | 100% | Test-first: +12 specs (reasoning models, timeline, evidence catalog/grounding, JSON extraction, Anthropic wrapper via injected fake, engine assembly + invalid-evidence filtering). Claude fully mocked — no key/spend. Zero regressions. |
| 2026-06-26 | LiveDatadogAdapter | 32 | 32 | 100% | Test-first: +8 specs (auth headers, query params, metric/event parsing, severity + source mapping, empty series, time window). All HTTP mocked via httpx.MockTransport — no keys/network. Zero regressions. |
| 2026-06-26 | DataSource + ReplayAdapter | 24 | 24 | 100% | Test-first: +13 specs (telemetry model, DataSource interface, replay canonical incident) written red, then implemented to green. Zero regressions. |
| 2026-06-26 | Foundation test baseline | 11 | 11 | 100% | Retrofitted tests for config (secret-free, capability flags, defaults) + app surface (healthz, status, index, chat). No pending tests yet. Env fix: pinned wheels for Python 3.14; dropped Jinja2 (page is static). |
