# OPEN-QUESTIONS.md

Unresolved items awaiting human input. Resolve → move the decision to `DECISIONS.md`.

## Open
- **Live Datadog config not taking effect (was reported as ".env not loading").** Diagnosed 2026-07-08: in the repo the `.env` loads fine (correct path, no CRLF/BOM, nothing shadowing) — it's a **contents** issue, the file is the untouched template (`COPILOT_DATA_SOURCE=replay`, all creds empty), so `replay`/no-creds is correct behaviour, not a loader bug. Added `scripts/check_env.py` (safe, no secret values) + `dotenv_path`/`dotenv_loaded` in `/api/status` to make this self-evident. **To go live on the work laptop:** run `python scripts/check_env.py`, then in `.env` set `COPILOT_DATA_SOURCE=datadog`, a `DATADOG_ACCESS_TOKEN` (or API+APP key), `DATADOG_TENANT_TAG`, `DATADOG_METRIC_NAMESPACES`, `DATADOG_SITE`, and restart. Still blocks live-validation of Iteration 2 until done on the laptop.
- **[RESOLVED 2026-08-05] The live demo now works end to end on real Datadog.** Root cause of the earlier zero-data readings: the 4-hour probe window. These metrics are **sparse** — measured at 36–153 datapoints per metric over **7 days** and zero over 4 hours. Over a 7-day window all 8 surveyed metrics returned data in all three aggregations. Fixes that made the live path work: `COPILOT_PLATFORM_DEFAULT_WINDOW_DAYS=7`, `DATADOG_ENV_TAG=kube_namespace`, `DATADOG_TENANT_TAG=tenant`, `DATADOG_CA_BUNDLE` populated (TLS failure was silently emptying the registry), plus the resolver/engine fixes below. **Verified:** 420-metric registry, 8 metric series with real data per question, a real LLM investigation citing 45 evidence entries, `/api/scopes` serving 2 environments + 10 tenants. **Sub-metric filter validated:** step 10 showed the base metric and its `.count`/`.sum`/`.max` siblings all return the same 36 points, so excluding the siblings loses nothing.
- **Old blocker text, kept for the record — no `ec.*` metric has returned any datapoint yet (2026-08-05, second probe run).** After restructuring the probe to target a **confirmed live-discovered** metric (`ec.alerting_service.alert_outbox_event_counter`), `avg:<metric>{*}` over a **4h weekday-business-hours window (09:08–13:08 UTC)** still returned **0 series**, and `/api/v2/metrics/<metric>/all-tags` returned **0 tags** — while `/api/v1/events` returns ~1000 events/hour, so the org and credential are demonstrably live. Two explanations remain, and the probe output so far **cannot** distinguish them:
  1. **Sparse emission** — these metrics report rarely; a 4h window simply missed them.
  2. **Wrong query form for the metric type** — a histogram-style metric's base name may not be queryable at all, with only the generated `.count/.sum/.min/.max` siblings holding the data.
  **Explanation 2 would invalidate the approved sub-metric filter** (`namespaces.is_stat_submetric`), because the excluded siblings would be the only queryable names. Do not treat that decision as settled until this resolves. `scripts/datadog_probe.py` now has **steps 8–10** built specifically to separate the two: a survey of up to 8 metrics × 3 aggregations (`avg:` / `sum:…as_count()` / `max:`) over a **7-day** window, a tag-key survey run only against a metric that actually has data, and a direct base-vs-`.count`/`.sum` comparison. **Re-run the probe and paste the output.** Until a metric returns points, the evidence catalog will be empty on live Datadog no matter how correct the registry is — this outranks every other open item.
- **Is `kube_namespace` really EC's tenant tag, and does `env` exist? (still open).** Both probe runs were inconclusive — the first targeted a non-reporting metric, the second a metric with no datapoints, so empty groupings prove nothing either time. Step 9 now runs the grouping only against a metric confirmed to have data, and prints the observed `tag_set` for `env`/`kube_namespace`/`service`/`host`/`environment`/`tenant` rather than checking one guess. Low practical risk regardless: listing `COPILOT_PLATFORM_TENANTS` explicitly bypasses tag discovery entirely, which is the intended demo config.
- **137 of 320 Terraform-extracted metrics are absent from the live 24h list (2026-08-05).** 43% of what the monitors repo references isn't currently reporting. This strongly vindicates the confirmed-reporting rule in `merged_metric_queries` (that much dead weight in the registry would have been selectable as evidence), but it's worth asking the platform owners whether those monitors are stale or the metrics were renamed.
- **Datadog scope-discovery query/response shape (partially validated).** `list_scopes` enumerates tag values by issuing `<in-scope metric>{scope} by {env|tenant}` and reading each series' `tag_set`. The request is accepted (HTTP 200) but `tag_set` parsing is still unverified because no probe has yet grouped a *live* metric. Whether `{(env:a OR env:b) AND tenant:x}` behaves as intended is also unconfirmed. Largely sidestepped by listing `COPILOT_PLATFORM_ENVIRONMENTS`/`_TENANTS` explicitly.

## Resolved
- **[RESOLVED 2026-08-05] Metric-name discovery endpoint shape.** `GET /api/v1/metrics?from=<epoch>` is **confirmed working** against the real org with a PAT: HTTP 200, body `{"metrics": [name, …]}`, **16,117 names returned, 1,297 matching `ec.*`**. The tolerant `{"data":[{"id":…}]}` fallback path was never needed but is kept as cheap insurance. Also confirmed: `/api/v1/validate` 403s with a PAT (expected — it's API-key-only), and `/api/v1/events` returns every field the adapter maps (`date_happened`, `title`, `text`, `alert_type`, `tags`, `id`) at ~1000 events/hour, so the existing `MAX_CATALOG_EVENTS=60` truncation matters.
- **What tag key is your "tenant"?** `DATADOG_TENANT_TAG` defaults to `tenant`; set it to your org's actual key (e.g. `customer`/`account`) when validating live.
- **Events multi-select filtering (known limitation).** The events API `tags` param ANDs its entries, so we only tag-filter a dimension when exactly one value is selected; multi-select env/tenant falls back to the time window for events. Revisit if events need precise multi-value scoping.
- **Which real Datadog signals matter most** for your environment (specific metrics, monitors, services)? Needed to make the LiveDatadogAdapter useful on *your* prod. Can default to common golden-signal metrics until specified.
- **Canonical demo incident realism** — is the generic deployment-induced latency story fine, or should the replay mimic a real past incident from your org?

## Resolved
- Run target → local, but able to connect to real production Datadog. (DECISIONS 2026-06-26)
- LLM access → Anthropic key available, cost-conscious mode. (DECISIONS 2026-06-26)
- UI → chat-style, backend as hero. (DECISIONS 2026-06-26)
- Secret handling → gitignored `.env`, never committed. (DECISIONS 2026-06-26)
