# Iteration 0 dependencies. All free, permissive (MIT/BSD/Apache-2.0) licenses.
# Versions chosen to ship prebuilt wheels for Python 3.14 (no compiler needed).
fastapi==0.138.1
uvicorn==0.49.0
anthropic==0.112.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.2.2
