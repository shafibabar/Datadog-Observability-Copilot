# Copy this file to ".env" and fill in your values for the demo.
# The real ".env" is gitignored and is NEVER committed. Leave this template empty.
#
#   cp .env.example .env      (then edit .env)

# --- Reasoning layer: how the app talks to Claude ---
# Two ways to run — you only need ONE:
#
#   (A) The "Claude Code" way (NO API key). If you have the `claude` CLI
#       installed and signed in (you're using it right now), leave everything
#       in this section blank. The app shells out to your local Claude Code
#       login. This is the default when no API key is set.
#
#   (B) The Anthropic API key way. Get a key at
#       https://console.anthropic.com/ -> Settings -> API Keys, and paste it here:
ANTHROPIC_API_KEY=

# Backend selection: "auto" (default: Claude CLI when no key above, else the API),
# "cli" (always use the Claude Code CLI), or "sdk" (always use the API key).
COPILOT_LLM_BACKEND=auto

# Model selection (cost-conscious defaults). Override only if you want.
# Cheaper model used for routine extraction/classification passes:
COPILOT_MODEL_FAST=claude-haiku-4-5-20251001
# Stronger model used for the main reasoning/narrative. Must be a current model id
# (e.g. claude-sonnet-5, claude-opus-4-8) or a CLI alias (sonnet/opus/haiku):
COPILOT_MODEL_DEEP=claude-sonnet-5

# --- Datadog (live data source — OPTIONAL) ---
# Leave blank to run on the replayed demo incident only.
#
# Preferred: a Personal Access Token (PAT) — a single standalone credential.
# Create one at: Datadog -> (your avatar) Personal Settings -> Access Tokens
#   -> "+ New Access Token" -> name it, set an expiry, grant read scopes, copy once.
DATADOG_ACCESS_TOKEN=
#
# Legacy alternative (still works, now considered legacy): API key + Application key.
#   https://app.datadoghq.com/organization-settings/api-keys          (API key)
#   https://app.datadoghq.com/organization-settings/application-keys  (Application key)
DATADOG_API_KEY=
DATADOG_APP_KEY=
# Your Datadog site/region (datadoghq.com, datadoghq.eu, us3.datadoghq.com, ...):
DATADOG_SITE=datadoghq.com
#
# Corporate networks that inspect HTTPS (Zscaler/Netskope/etc.) re-sign traffic with
# a private root CA that Python doesn't trust by default -> "CERTIFICATE_VERIFY_FAILED".
# Point this at your corporate CA bundle (PEM) to fix it securely:
DATADOG_CA_BUNDLE=
# Last resort only (insecure — disables TLS verification): set to 0.
DATADOG_VERIFY_SSL=1
#
# Scope dropdowns (Environment / Tenant / Duration):
#   "env" is Datadog's standard environment tag. "tenant" is NOT a native Datadog
#   concept, so tell the app which tag key represents your tenant (e.g. tenant,
#   customer, account, org):
DATADOG_TENANT_TAG=tenant
#   ...and which tag key represents the ENVIRONMENT. `env` is only a convention,
#   NOT guaranteed: a real org was found carrying no `env` tag at all (grouping by
#   it returns the single placeholder series `env:N/A`) and expressing environment
#   as `kube_namespace` instead. If your Environment filter matches nothing, this
#   is almost certainly why. Confirm with step 9 of scripts/datadog_probe.py.
#   e.g. for the EC platform: DATADOG_ENV_TAG=kube_namespace
DATADOG_ENV_TAG=env
#   (Tenant/environment values are normally listed explicitly below via
#   COPILOT_PLATFORM_TENANTS / _ENVIRONMENTS. Only if you leave those blank does the
#   app enumerate them live, by grouping an in-scope metric by the tag.)
#
# --- Metric scope: THE setting that decides which metrics exist for the copilot ---
# Comma-separated wildcard patterns naming the metric namespaces in scope. This is
# an allowlist: a live org has thousands of metrics, and only these are ever read,
# discovered, or reasoned over. It drives all three sources of metrics at once —
# live discovery from Datadog, extraction from the Terraform monitors repo, and the
# vocabulary the relevance guard treats as on-topic.
#   e.g. one namespace : ec.*
#   e.g. two namespaces: ec.*, ea.*
#   e.g. narrower      : ec.quota_*
# Leave blank to keep the old behavior (no namespace filtering; infra defaults apply).
DATADOG_METRIC_NAMESPACES=
#
# Which metrics the copilot reasons over, as JSON {logical_name: datadog_query}.
# Leave blank to let DATADOG_METRIC_NAMESPACES above resolve the registry for you.
# Entries here always win, and stay in scope even outside those namespaces, e.g.:
#   DATADOG_METRIC_QUERIES={"api.latency.p95":"p95:trace.http.request.duration{*}","errors":"sum:trace.http.request.errors{*}.as_rate()"}
DATADOG_METRIC_QUERIES=

# --- Platform scope: narrow the Copilot to ONE known platform (OPTIONAL) ---
# Fill these in once you know a specific platform's telemetry shape (few
# environments/tenants, known metric/log/trace names). They (a) become the only
# choices offered by the @ scope menu in the composer, and (b) extend the
# relevance guard's on-topic vocabulary so it recognizes this platform's own
# terms, not just generic words like "latency"/"cpu". Leave all blank to keep
# the current generic behavior (broader guard vocabulary, no default narrowing).
# See README.md -> "Narrow the Copilot to one platform" for exactly where to
# find each of these in the Datadog UI.
#
# Comma-separated env tag values this platform reports, e.g.: production,staging
COPILOT_PLATFORM_ENVIRONMENTS=
# Comma-separated tenant values (tag key from DATADOG_TENANT_TAG above), e.g.: acme,globex
COPILOT_PLATFORM_TENANTS=
# Comma-separated metric names this platform emits, e.g.: myplatform.request.latency,myplatform.request.errors
COPILOT_PLATFORM_METRICS=
# Comma-separated log source/service values this platform's logs carry, e.g.: myplatform-api,myplatform-worker
COPILOT_PLATFORM_LOG_SOURCES=
# Comma-separated APM/trace service names this platform is instrumented under, e.g.: myplatform-api,myplatform-checkout
COPILOT_PLATFORM_TRACE_SERVICES=
# Default lookback window (days) when a question doesn't specify a duration via
# the @ scope menu. Capped by the existing 7-day max span.
# NOTE: EC's ec.* metrics are SPARSE — measured at 36-153 datapoints per metric
# over 7 days, and ZERO over a 4-hour window. A short default window will produce
# empty evidence. 7 (the enforced maximum span) is the safe choice for live demos.
COPILOT_PLATFORM_DEFAULT_WINDOW_DAYS=7

# --- App ---
# Which data source the app starts with: "replay" (scripted demo) or "datadog" (live).
# >>> To use your real Datadog org, set this to: datadog
COPILOT_DATA_SOURCE=replay
# Where the Investigation Workspace database is stored (gitignored). Optional.
COPILOT_WORKSPACE_DB=data/workspace.db
